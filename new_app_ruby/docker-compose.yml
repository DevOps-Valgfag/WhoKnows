version: "3.9"

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SESSION_SECRET=${SESSION_SECRET}
    volumes:
      - .:/app
      - whoknows_db:/app/db
    env_file:
      - .env

volumes:
  whoknows_db: