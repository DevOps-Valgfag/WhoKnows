<!DOCTYPE html>
<html>
<head>
  <title>Weather Forecast for <%= @city %></title>
</head>
<body>

  <h1>Weather Forecast for <%= @city %></h1>

  <h2>Current Weather</h2>
  <p>
    Currently: <strong><%= @current_condition['temp_C'] %>°C</strong>,
    feels like <%= @current_condition['FeelsLikeC'] %>°C.
  </p>
  <p><%= @current_condition['weatherDesc'][0]['value'] %></p>

  <hr>

  <h2>3-Day Forecast</h2>
  <div class="forecast-container">
    <% @forecast.each do |day| %>
      <div class="day">
        <div class="date"><%= Date.parse(day['date']).strftime('%A, %b %d') %></div>
        <div>Max: <strong><%= day['maxtempC'] %>°C</strong></div>
        <div>Min: <strong><%= day['mintempC'] %>°C</strong></div>
        <div>Avg: <%= day['avgtempC'] %>°C</div>
        <p><%= day['hourly'][4]['weatherDesc'][0]['value'] %></p> <!-- Description for midday -->
      </div>
    <% end %>
  </div>

</body>
</html>